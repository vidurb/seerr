apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ include "seerr.fullname" . }}
  labels:
    {{- include "seerr.labels" . | nindent 4 }}
spec:
  serviceName: {{ include "seerr.fullname" . }}
  selector:
    matchLabels:
      {{- include "seerr.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "seerr.labels" . | nindent 8 }}
        {{- with .Values.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "seerr.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      {{- if or .Values.volumePermissions.enabled (eq (include "seerr.settings.helmEnabled" .) "true") .Values.extraInitContainers }}
      initContainers:
        {{- if .Values.volumePermissions.enabled }}
        - name: volume-permissions
          image: {{ include "seerr.volumePermissions.image" . | quote }}
          imagePullPolicy: {{ .Values.volumePermissions.image.pullPolicy }}
          command:
            {{- toYaml .Values.volumePermissions.command | nindent 12 }}
          resources:
            {{- toYaml .Values.volumePermissions.resources | nindent 12 }}
          securityContext:
            runAsUser: 0
            runAsGroup: 0
            runAsNonRoot: false
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
          volumeMounts:
            - name: config
              mountPath: /app/config
              subPath: {{ .Values.config.persistence.subPath }}
        {{- end }}
        {{- if eq (include "seerr.settings.helmEnabled" .) "true" }}
        - name: apply-settings
          image: {{ include "seerr.settings.initImage" . | quote }}
          imagePullPolicy: {{ .Values.volumePermissions.image.pullPolicy }}
          command:
            - /bin/sh
            - -c
            - cp /settings-source/{{ default "settings.json" ((.Values.settings | default dict).existingSecretKey) }} /app/config/settings.json && chown 1000:1000 /app/config/settings.json
          resources:
            {{- toYaml .Values.volumePermissions.resources | nindent 12 }}
          securityContext:
            runAsUser: 0
            runAsGroup: 0
            runAsNonRoot: false
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
          volumeMounts:
            - name: settings-source
              mountPath: /settings-source
              readOnly: true
            - name: config
              mountPath: /app/config
              subPath: {{ .Values.config.persistence.subPath }}
        {{- end }}
        {{- with .Values.extraInitContainers }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- end }}
      containers:
        - name: {{ .Chart.Name }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: {{ include "seerr.image" . | quote }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 5055
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /api/v1/settings/public
              port: http
            {{- if .Values.probes.livenessProbe.initialDelaySeconds }}
            initialDelaySeconds: {{ .Values.probes.livenessProbe.initialDelaySeconds }}
            {{- end }}
            {{- if .Values.probes.livenessProbe.periodSeconds }}
            periodSeconds: {{ .Values.probes.livenessProbe.periodSeconds }}
            {{- end }}
            {{- if .Values.probes.livenessProbe.timeoutSeconds }}
            timeoutSeconds: {{ .Values.probes.livenessProbe.timeoutSeconds }}
            {{- end }}
            {{- if .Values.probes.livenessProbe.successThreshold }}
            successThreshold: {{ .Values.probes.livenessProbe.successThreshold }}
            {{- end }}
            {{- if .Values.probes.livenessProbe.failureThreshold }}
            failureThreshold: {{ .Values.probes.livenessProbe.failureThreshold }}
            {{- end }}
          readinessProbe:
            httpGet:
              path: /api/v1/settings/public
              port: http
            {{- if .Values.probes.readinessProbe.initialDelaySeconds }}
            initialDelaySeconds: {{ .Values.probes.readinessProbe.initialDelaySeconds }}
            {{- end }}
            {{- if .Values.probes.readinessProbe.periodSeconds }}
            periodSeconds: {{ .Values.probes.readinessProbe.periodSeconds }}
            {{- end }}
            {{- if .Values.probes.readinessProbe.timeoutSeconds }}
            timeoutSeconds: {{ .Values.probes.readinessProbe.timeoutSeconds }}
            {{- end }}
            {{- if .Values.probes.readinessProbe.successThreshold }}
            successThreshold: {{ .Values.probes.readinessProbe.successThreshold }}
            {{- end }}
            {{- if .Values.probes.readinessProbe.failureThreshold }}
            failureThreshold: {{ .Values.probes.readinessProbe.failureThreshold }}
            {{- end }}
          {{- if .Values.probes.startupProbe }}
          startupProbe:
            {{- toYaml .Values.probes.startupProbe | nindent 12 }}
          {{- end }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          {{- with .Values.extraEnv }}
          env:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.extraEnvFrom }}
          envFrom:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          volumeMounts:
            - name: config
              mountPath: /app/config
              subPath: {{ .Values.config.persistence.subPath }}
          {{- with .Values.volumeMounts }}
            {{- toYaml . | nindent 12 }}
          {{- end }}
      volumes:
        - name: config
          persistentVolumeClaim:
            claimName: {{ if .Values.config.persistence.existingClaim }}{{ .Values.config.persistence.existingClaim }}{{- else }}{{ include "seerr.configPersistenceName" . }}{{- end }}
      {{- if eq (include "seerr.settings.helmEnabled" .) "true" }}
        - name: settings-source
          secret:
            secretName: {{ include "seerr.settings.sourceSecretName" . }}
      {{- end }}
      {{- with .Values.volumes }}
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
